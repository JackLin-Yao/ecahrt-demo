import Vue from 'vue'
import { Button, Input, Card, Row, Col,DatePicker, MenuItem, Menu, RadioButton, RadioGroup } from 'element-ui'


Vue.use(Button)
  .use(Card)
  .use(Row)
  .use(Col)
  .use(Menu)
  .use(MenuItem)
  .use(RadioGroup)
  .use(RadioButton)
  .use(DatePicker)
  .use(Input);
